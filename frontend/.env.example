# Base URL of the FastAPI backend (no trailing slash).
# In development, Vite proxies /api → localhost:8000 so the default works.
# In production builds, set this to your backend URL, e.g. http://localhost:8000
VITE_API_BASE_URL=http://localhost:8000
